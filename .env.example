VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Monetization kill switch — xem docs/MONETIZATION.md §9.
#   false (mặc định) → mọi address full 3 module, không gate, không query DB
#   true             → bật gate + query get_address_entitlement (guest vẫn bypass)
VITE_MONETIZATION_ENABLED=false
