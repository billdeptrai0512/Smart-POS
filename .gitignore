# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel

.env
.env.local
.env.*.local

CLAUDE.md
PROJECT.md

# Claude Code session state (per-machine)
.claude/worktrees/
.claude/settings.local.json
.env*.local

# Supabase CLI scratch
supabase/.temp/

# Local debug / diagnostic one-offs
scratch/

# Trình cài đặt / binary: từng lỡ commit TeamViewer_Setup_x64.exe (79MB) vào
# src/assets rồi phải viết lại lịch sử để gỡ. Git không phải chỗ chứa installer.
*.exe
*.msi
*.dmg

# Thư mục tạm của trình duyệt khi tải file — nguồn của mớ .exe nói trên, và nó
# còn chứa cả file không đuôi .exe nên chặn theo tên thư mục mới đủ.
scoped_dir*/
